# Secrets das Edge Functions — copie para supabase/functions.env e preencha.
# Aplicados no Supabase com: supabase secrets set --env-file supabase/functions.env
# Este arquivo (.example) pode ir pro git; o functions.env real NÃO (está no .gitignore).

KOMMO_TOKEN=token-de-longa-duracao-do-kommo
KOMMO_SUBDOMAIN=zeandre
CLICKUP_TOKEN=pk_xxxxxxxxx
